services:
  ollama:
    image: ollama/ollama:latest
    ports:
      - "11434:11434"
    volumes:
      - ollama-data:/root/.ollama
    healthcheck:
      test: ["CMD", "ollama", "list"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 20s

  devsecops:
    build:
      context: .
    image: agia-devsecops:latest
    environment:
      OLLAMA_HOST: http://ollama:11434
      OLLAMA_MODEL: llama3.1
      DEVSECOPS_MAX_CYCLES: "1"
      LOG_LEVEL: INFO
    depends_on:
      ollama:
        condition: service_started
    volumes:
      - ../:/workspace:ro
    command: ["/workspace", "--show-report"]
    restart: "no"

volumes:
  ollama-data:
